// Prisma schema for the personal job-search dashboard.
// Learn more: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client"
  output   = "../app/generated/prisma"
}

datasource db {
  provider = "mysql"
}

// Status is a plain string (validated in app code via zod) rather than a
// native enum, kept intentionally simple for this single-table personal app.
// Values: a_postuler | postule | relance | entretien | refuse | offre

model Job {
  id                String    @id @default(cuid())
  title             String
  company           String
  location          String?
  remoteType        String?
  salaryMin         Int?
  salaryMax         Int?
  stack             String?   @db.Text
  url               String    @unique @db.VarChar(768)
  source            String?
  postedAt          DateTime?
  description       String?   @db.Text
  status            String    @default("a_postuler")
  notes             String?   @db.Text
  cvGeneratedAt     DateTime?
  letterGeneratedAt DateTime?
  appliedAt         DateTime?
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt

  @@index([status])
  @@index([postedAt])
}
